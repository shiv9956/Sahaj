{
  "name": "sahaj-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx pnpm --recursive --parallel run dev",
    "build": "npx pnpm --recursive run build",
    "test": "vitest run",
    "test:smoke": "ts-node scripts/smoke/cognee.ts && ts-node scripts/smoke/sarvam-chat.ts && ts-node scripts/smoke/n8n-webhook.ts",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "ts-node": "^10.9.2",
    "vitest": "^1.4.0",
    "dotenv": "^16.4.5"
  }
}
